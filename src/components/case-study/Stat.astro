---
interface Props {
  value: string;
  label: string;
}

const { value, label } = Astro.props;
---

<div class="cs-stat">
  <p class="cs-stat-value">{value}</p>
  <p class="cs-stat-label">{label}</p>
</div>
