---
const { dividerType = "primary" } = Astro.props;
---

<hr class={`divider ${dividerType}`} />
<style>
  .divider {
    border-radius: 1.5rem;
    border-width: 1.5px;
    inline-size: 100%;
  }

  .primary {
    border-color: rgba(var(--secondary-color), 1);
  }

  .secondary {
    border-color: rgba(var(--forground), 1);
  }

  .tertiary {
    border-color: rgba(var(--floating-color), 1);
    border-width: 1.5px;
  }
</style>
